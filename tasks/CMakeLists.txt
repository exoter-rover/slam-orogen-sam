# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(samTaskLib)
ADD_LIBRARY(${SAM_TASKLIB_NAME} SHARED 
    ${SAM_TASKLIB_SOURCES})
add_dependencies(${SAM_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${SAM_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${SAM_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SAM_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SAM_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${SAM_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${SAM_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SAM_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SAM_TASKLIB_HEADERS}
    DESTINATION include/orocos/sam)

